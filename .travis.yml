sudo: false
language: java
env:
  - PLAY_VERSION=2.4.2
  - ACTIVATOR_VERSION=1.3.5
jdk: oraclejdk8
before_script:
  - wget http://downloads.typesafe.com/typesafe-activator/${ACTIVATOR_VERSION}/typesafe-activator-${ACTIVATOR_VERSION}-minimal.zip
  - unzip typesafe-activator-${ACTIVATOR_VERSION}-minimal.zip
script:
  - ./activator-${ACTIVATOR_VERSION}-minimal/activator "testOnly ApplicationTest"
  - ./activator-${ACTIVATOR_VERSION}-minimal/activator "testOnly archive.search.SearchTest"